# for fallback function when the requested micro service is down (hystrix)
feign.hystrix.enabled=true
feign.client.config.default.connectTimeout= 160000000
feign.client.config.default.readTimeout= 160000000

server.port=${PORT:0}
spring.application.name=users-ws
eureka.client.serviceUrl.defaultZone=http://localhost:8010/eureka
spring.devtools.restart.enabled=true
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

spring.h2.console.enabled=true
# h2 db server has a built in firewall, and because we are connecting to users ms, through API gateway, we need to enable connections from other addresses
spring.h2.console.settings.web-allow-others=true
spring.h2.console.path=/h2-console
#spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.url=jdbc:mysql://localhost:3306/ms_photo_app?serverTimezone=UTC
spring.datasource.username=chan
spring.datasource.password=chan
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

gateway.ip=127.0.0.1
token.expiration_time=864000000
token.secret=askjjdnk18a09sdj1ajsndlk
login.url.path=/users/login

spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

management.endpoints.web.exposure.include=mappings

logging.level.com.u4.api.users.data.AlbumsServiceClient=DEBUG

